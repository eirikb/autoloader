var bot = require('bot.js');


bot.onTrigger(this, 'triggers', function(from, to, msg) {
    bot.client.say(to, bot.triggers.join(' '));
};

exports.destruct = function() {
    bot.removeListeners(this);
};


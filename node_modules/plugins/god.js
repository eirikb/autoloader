var bot = require('bot.js'),
t = require('translate.js'),
g = require('get.js'),
$ = requrie('jquery'),
_ = require('underscore');

var say = s = function(to, msg) {
    bot.client.say(to, msg);
};

bot.onTrigger(this, 'e', function(from, to, msg) {
    try {
        console.log('%sTrying to run %s...', from, msg);
        var e = eval(message);
        if (typeof e !== 'undefined') {
            bot.client.say(to, e);
        }
    } catch(err) {
        console.log(err);
    }
};

exports.destruct = function() {
    bot.removeListeners(this);
};

